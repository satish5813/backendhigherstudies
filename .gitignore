node_modules/
dist/
build/
.env
.env.local
# Every .env variant EXCEPT the committed template. .env.backup held a live
# SMTP password and API key and was staged by the first .
.env.*
!.env.example
*.log
.DS_Store
uploads/
coverage/
.local-db/

# Application-proof screenshots. Student PII — never versioned.
server/private/

# Student personal data: names, mobile numbers, dates of birth, gender,
# personal emails, CGPAs for 656 real people. Read from disk by
# import-cohorts.js; it must never enter a repository.
server/src/db/data/
